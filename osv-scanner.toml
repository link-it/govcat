# OSV Scanner configuration file
# Documentazione: https://google.github.io/osv-scanner/configuration/
#
# Suppression delle vulnerabilita' che non possono essere risolte sulla linea di
# versione attualmente in uso. Le suppression relative a Spring Boot 3.3.x /
# Spring Framework 6.1.x / Spring Security 6.3.x (CVE-2025-41242, CVE-2025-41249,
# CVE-2026-22735, CVE-2026-22737, CVE-2026-22732) e a jackson-databind 2.21.4
# (CVE-2026-54515) sono state rimosse: con Spring Boot 4.0.7 / Spring Framework
# 7.0.8 / Spring Security 7.0.6 e jackson-databind 2.21.5 le versioni in uso non
# sono piu' affette.
#
# IMPORTANTE: rivedere periodicamente le suppression sotto. La data
# ignoreUntil e' un promemoria per forzare la verifica al successivo
# refresh delle dipendenze. Ogni suppression aggiunta o rimossa qui va
# allineata con quelle di OWASP Dependency-Check, che sono elencate sia in
# tools/owasp/suppression/ che nei <suppressionFiles> del pom.xml radice (job
# backend) e negli argomenti --suppression di .github/workflows/main.yml (job
# frontend).

[[IgnoredVulns]]
id = "GHSA-5p2g-fcmc-qvqq"
ignoreUntil = 2026-11-30
reason = """
image-size 2.0.2 (CVE-2025-71329, DoS per loop infinito nei parser JXL/HEIF): non esiste
alcuna versione fixata pubblicata (l'advisory riporta last_affected 2.0.2, che e' anche
l'ultima release sul registry npm), quindi l'upgrade non e' possibile.
Impatto nullo su GovCat: image-size e' una dipendenza transitiva di sola build
(@angular-devkit/build-angular -> less -> image-size), non viene inclusa nel bundle
distribuito e riceve in input solo le immagini presenti nei sorgenti del progetto,
non input remoto non fidato. Vulnerabilita' gia' soppressa anche in OWASP
Dependency-Check (vedi tools/owasp/suppression/image-size.xml). Rimuovere entrambe le
suppression appena esce una release fixata.
"""

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
ignoreUntil = 2026-11-30
reason = """
image-size 2.0.2 (CVE-2025-71330, DoS per loop infinito nel parser ICNS): stessa
situazione di GHSA-5p2g-fcmc-qvqq, nessuna versione fixata pubblicata e dipendenza di
sola build (less), non presente nel bundle distribuito. Vulnerabilita' gia' soppressa
anche in OWASP Dependency-Check (vedi tools/owasp/suppression/image-size.xml).
Rimuovere entrambe le suppression appena esce una release fixata.
"""
